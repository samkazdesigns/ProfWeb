# Source material for projects — reports, presentations, raw images.
# Kept locally so Claude can read it, but never pushed to the public repo.
# Web-ready images that belong ON the site live in images/ instead.
Project Info/
